scripts:
  prepare-images:
    - log: nothing to do as images are already available in quay.io
    - signal: QUARKUS_IMAGES_BUILD_COMPLETED

states:
  IS_NATIVE: true
  QUARKUS_IMAGE: "quay.io/quarkus-reactive-beer/beer:native-latest"

  # change these values
  CONTAINER_RUNTIME_PARAMS:
